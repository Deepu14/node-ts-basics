# node-ts-basics

A REST API built with Node.js, Express, and TypeScript.

## Installation
npm install
npm start

## Endpoints
GET /
GET /health
GET /users
POST /users
DELETE /users/:name

## Tests
npx tsx src/tests/userService.test.ts
